{
  "name": "hd-audio-transcriber",
  "version": "0.1.0",
  "description": "A macOS app that turns audio into a transcript with speaker labels and timestamps",
  "private": true,
  "license": "MIT",
  "main": "dist/main.js",
  "scripts": {
    "assets": "mkdir -p dist/renderer && cp src/renderer/index.html src/renderer/styles.css dist/renderer/",
    "build": "tsc && npm run assets",
    "start": "npm run build && electron .",
    "dev": "bash scripts/dev.sh",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "electron": "^44.3.0",
    "typescript": "^7.0.2"
  }
}

# 依赖
node_modules/

# 本机配置：含 HuggingFace token，绝不入库
config.json

# 打包产物
dist/
out/
*.app

# 由 build/make-icon.sh 从 icon-source.png 生成，M6 打包时重建。
# 派生文件留在库里只会悄悄过期 —— 曾经有几个 commit 带着旧版图标发出去，
# 因为没有任何机制强制它跟源文件同步。
build/icon.icns

# 测试音频
tmp/
output/
samples/
*.m4a
*.mp3
*.wav
*.flac

# 转录产物：可能含真人访谈内容
*.srt
*.vtt
*.docx
transcript*.json

# Claude Code 本机设置
.claude/settings.local.json

# macOS / 编辑器
.DS_Store
*.log
.vscode/
.idea/
